<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choose A RAM - Comp@Tibility</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <link rel="stylesheet" href="/mainStyles.css">
    <link rel="stylesheet" href="/build.css">
</head>
<body class="d-flex flex-column min-vh-100 bg-dark text-white">
<%- include('../partials/header') %>

<div class="container mt-4">
    <h1 class="text-center mb-4">Search Your RAM</h1>

    <div class="d-none d-lg-block table-responsive rounded-3">
        <table class="table table-dark table-striped table-hover text-center align-middle">
            <thead class="table-primary">
            <tr>
                <th>#</th>
                <th>Brand</th>
                <th>Model</th>
                <th>Type</th>
                <th>Capacity (GB)</th>
                <th>Speed (GHz)</th>
                <th>Sticks</th>
                <th class="actionTh">Action</th>
            </tr>
            </thead>
            <tbody>
            <% rams.forEach((ram, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td>
                        <% if (brandsLogos[ram.brand]) { %>
                            <img src="<%= brandsLogos[ram.brand].logo %>" alt="<%=ram.brand %> Logo"
                                 height="25" style="background-color: white; padding: 5px; border-radius: 5px;">
                        <% } %>
                    </td>
                    <td><%= ram.model %></td>
                    <td><%= ram.type %></td>
                    <td><%= ram.capacity %></td>
                    <td><%= ram.speed %></td>
                    <td><%= ram.num_sticks %></td>
                    <td>
                        <button class="btn btn-success choose-component"
                                data-component="memory"
                                data-id="<%= ram.id %>"
                                data-brand="<%= ram.brand %>"
                                data-model="<%= ram.model %>">
                            Choose
                        </button>
                    </td>
                </tr>
            <% }) %>
            </tbody>
        </table>
    </div>

    <!-- Stacked cards on small screens -->
    <div class="d-lg-none">
        <% rams.forEach((ram) => { %>
            <div class="card mb-3 bg-dark text-white border-light">
                <div class="card-body text-center">
                    <h3 class="card-title text-center"><strong><%= ram.model %></strong></h3>
                    <p class="card-text text-center">
                        <strong>Brand:</strong> <%= ram.brand %> <br>
                        <strong>Type:</strong> <%= ram.type %> <br>
                        <strong>Capacity (GB):</strong> <%= ram.capacity %> <br>
                        <strong>Speed (GHz):</strong> <%= ram.speed %> <br>
                        <strong>Sticks:</strong> <%= ram.num_sticks %><br>
                    </p>
                    <button class="btn btn-success btn-sm choose-component"
                            data-component="memory"
                            data-id="<%= ram.id %>"
                            data-brand="<%= ram.brand %>"
                            data-model="<%= ram.model %>">
                        Choose
                    </button>
                </div>
            </div>
        <% }) %>
    </div>
    
</div>


<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
<script src="/scripts/allowClickBasedOnUrl.js"></script>
</body>
</html>
