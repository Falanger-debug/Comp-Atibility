<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choose A Motherboard - Comp@Tibility</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <link rel="stylesheet" href="/mainStyles.css">
    <link rel="stylesheet" href="/build.css">
</head>
<body class="d-flex flex-column min-vh-100 bg-dark text-white">
<%- include('../partials/header') %>
<div class="container mt-4">
    <h1 class="text-center mb-4">Search Your Motherboard</h1>

    <div class="d-none d-lg-block table-responsive rounded-3">
        <table class="table table-dark table-striped table-hover text-center align-middle">
            <thead class="table-primary">
            <tr>
                <th>#</th>
                <th>Brand</th>
                <th>Model</th>
                <th>Socket</th>
                <th>Chipset</th>
                <th>Form Factor</th>
                <th>Max Memory (GB)</th>
                <th>Memory Slots</th>
                <th>Memory Type</th>
                <th>Slot</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            <% motherboards.forEach((mobo, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td>
                        <% if (brandsLogos[mobo.brand]) { %>
                            <img src="<%= brandsLogos[mobo.brand].logo %>" alt="<%=mobo.brand %> Logo"
                                 height="25" class="bg-white p-1 rounded">
                        <% } %>
                    </td>
                    <td><%= mobo.model %></td>
                    <td><%= mobo.socket %></td>
                    <td><%= mobo.chipset %></td>
                    <td><%= mobo.form_factor %></td>
                    <td><%= mobo.max_memory %></td>
                    <td><%= mobo.memory_slots %></td>
                    <td><%= mobo.memory_type %></td>
                    <td><%= mobo.slot %></td>
                    <td>
                        <button class="btn btn-success btn-sm choose-component"
                                data-component="motherboard"
                                data-id="<%= mobo.id %>"
                                data-brand="<%= mobo.brand %>"
                                data-model="<%= mobo.model %>">
                            Choose
                        </button>
                    </td>
                </tr>
            <% }) %>
            </tbody>
        </table>
    </div>

    <!-- Stacked cards on small screens -->
    <div class="d-lg-none">
        <% motherboards.forEach((mobo) => { %>
            <div class="card mb-3 bg-dark text-white border-light">
                <div class="card-body text-center">
                    <h3 class="card-title text-center"><strong><%= mobo.model %></strong></h3>
                    <p class="card-text text-center">
                        <strong>Brand:</strong> <%= mobo.brand %> <br>
                        <strong>Socket:</strong> <%= mobo.socket %> <br>
                        <strong>Chipset:</strong> <%= mobo.chipset %> <br>
                        <strong>Form Factor:</strong> <%= mobo.form_factor %> <br>
                        <strong>Max Memory:</strong> <%= mobo.max_memory %> GB<br>
                        <strong>Memory Slots:</strong> <%= mobo.memory_slots %><br>
                        <strong>Memory Type:</strong> <%= mobo.memory_type %><br>
                        <strong>Slot:</strong> <%= mobo.slot %><br>
                    </p>
                    <button class="btn btn-success btn-sm choose-component"
                            data-component="motherboard"
                            data-id="<%= mobo.id %>"
                            data-brand="<%= mobo.brand %>"
                            data-model="<%= mobo.model %>">
                        Choose
                    </button>
                </div>
            </div>
        <% }) %>
    </div>
</div>



<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
<script src="/scripts/allowClickBasedOnUrl.js"></script>
</body>
</html>
