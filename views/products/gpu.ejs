<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choose A GPU - Comp@Tibility</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <link rel="stylesheet" href="/mainStyles.css">
    <link rel="stylesheet" href="/build.css">
</head>
<body class="d-flex flex-column min-vh-100 bg-dark text-white">
<%- include('../partials/header') %>

<div class="container mt-4">
    <h1 class="text-center mb-4">Search Your GPU</h1>

    <div class="table-responsive rounded-3">
        <table class="table table-dark table-striped table-hover text-center align-middle">
            <thead class="table-primary">
            <tr>
                <th>#</th>
                <th>Brand</th>
                <th>Model</th>
                <th>VRAM</th>
                <th>Interface</th>
                <th>TDP (W)</th>
                <th>Recommended PSU (W)</th>
                <th>Length (mm)</th>
                <th class="actionTh">Action</th>
            </tr>
            </thead>
            <tbody>
            <% gpus.forEach((gpu, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td>
                        <% if (brandsLogos[gpu.brand]) { %>
                            <img src="<%= brandsLogos[gpu.brand].logo %>" alt="<%=gpu.brand %> Logo"
                                 height="25" style="background-color: white; padding: 5px; border-radius: 5px;">
                        <% } %>
                    </td>
                    <td><%= gpu.model %></td>
                    <td><%= gpu.vram %></td>
                    <td><%= gpu.interface %></td>
                    <td><%= gpu.tdp %></td>
                    <td><%= gpu.recommended_psu %></td>
                    <td><%= gpu.length %></td>
                    <td>
                        <button class="btn btn-success choose-component"
                                data-component="video-card"
                                data-id="<%= gpu.id %>"
                                data-brand="<%= gpu.brand %>"
                                data-model="<%= gpu.model %>">
                            Choose
                        </button>
                    </td>
                </tr>
            <% }) %>
            </tbody>
        </table>
    </div>
</div>


<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
<script src="/scripts/allowClickBasedOnUrl.js"></script>
</body>
</html>
